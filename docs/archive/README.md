# 归档文档（已失效，仅保追溯）

> 本目录的文档**不反映项目现状**，禁止作为实施依据。每条归档都写明了内容去向。

## 归档清单

### `ARCHITECTURE-2026-09-10.md`

- **原文**：`docs/架构分析.md`（自述「更新于 2026-09-10，对应 v0.2.2」）
- **归档日期**：2026-09-11
- **为何归档**：写的是 v0.2.2 的形态，与当前 v0.3.4 有实质偏差（测试项数、迁移账、依赖口径、
  桌面分发方式都已变）。它取代的更早一版（pywebview + 纯 Python 单体）同样已失效，
  说明「单份架构分析」这种载体本身就会过期。
- **内容去向**：
  - 总体架构图与四条设计底线 → `docs/EXTENSION-PLANE.md`、`docs/adr/0002`（浏览器分配政策）
  - 端口与进程分工表 → `docs/API.md`（§0 拓扑）与 `docs/CONFIG.md`（§1 端口与进程）
  - 模块地图 → `docs/AGENT.md`（§2 代码地图，按实际行数校正）
  - 三条关键数据流 → `docs/EXTENSION-PLANE.md`（快捷登录流）与 `docs/API.md`（托管/监听流）
  - 数据层与版本发布 → `docs/SCHEMA.md`、`docs/adr/0001`、`docs/adr/0009`
  - 「已下线（源码已删，勿恢复）」清单 → `docs/AGENT.md`（§6 红线）
  - 排障速查与环境坑 → `docs/AGENT.md`（§5 排障速查）

### `SESSION-DIGEST-2026-09-10.md`

- **原文**：`docs/SESSION-DIGEST.md`（自述「更新于 2026-09-10」，自述当前 v0.2.2）
- **归档日期**：2026-09-11
- **为何归档**：它是**跨会话上下文恢复用**的压缩摘要，价值密度最高的部分是 20 条实测环境坑、
  四条验收基线、以及各版本的实锤断点记录（如 Fernet 键位写反、扩展游标不持久化）。
  这类内容应该沉淀进常驻文档，而不是留在一份会被不断覆盖的「速查」里。
- **内容去向**：
  - 20 条环境坑 + 验收基线 → `docs/AGENT.md`（§5 排障速查、§7 验收基线）
  - 各版本实锤断点（0.2.4 游标 / 0.2.6 Fernet 键位 / 0.2.21 权限声明式 / 0.2.24 长轮询等）
    → 根 `CHANGELOG.md` 与 `docs/EXTENSION-PLANE.md`（§5 私有改造史）
  - 已下线清单 → `docs/AGENT.md`（§6 红线）
  - 打包分发链路 → `docs/adr/0009` 与 `CONTRIBUTING.md`

## 为何保留而不是删除

这两份文档记录了大量**只在真机复现过的教训**（架构分析 §8 的 10 条排障条目、SESSION-DIGEST 的
20 条环境坑），其中多条无法从当前代码反推出来。内容已全部搬入常驻文档，但保留原文是为了：
1. 溯源——某条结论当初是在哪次事故里得出的；
2. 校验——搬移过程中若发生失真，可对照原文；
3. 反面教材——它们证明了「一份自述版本的架构文档」会在 1 个 MINOR 版本内失效，
   所以现行体系把内容按**变更频率**拆开（ADR 记不变的决策、CONFIG/SCHEMA/API 记随代码走的事实）。
