# 用户手册（USER-MANUAL）

> 面向**使用者**（阿克索实施工程师）。目标：把工作台的五个模块用起来，并知道数据放在哪、什么情况下会出错。
>
> 想了解设计取舍看 [docs/adr/](adr/)；想调接口看 [docs/API.md](API.md)；
> 想改行为/排障看 [docs/CONFIG.md](CONFIG.md) 与 [AGENT.md](../AGENT.md)。

## 目录

1. [安装与首次启动](#1-安装与首次启动)
2. [界面总览](#2-界面总览)
3. [账号中心](#3-账号中心)
4. [快捷登录与托管浏览器](#4-快捷登录与托管浏览器)
5. [平台洞察](#5-平台洞察)
6. [配置工厂](#6-配置工厂)
7. [对话与设置](#7-对话与设置)
8. [监听会话（Monitor）](#8-监听会话monitor)
9. [自动更新](#9-自动更新)
10. [数据位置与安全须知](#10-数据位置与安全须知)
11. [常见问题（FAQ）](#11-常见问题faq)

---

## 1. 安装与首次启动

### 1.1 安装

1. 运行 `AksoWorkbench-<版本>-setup.exe`（约 350 MB）。
   - 可自选安装目录；**默认不需要管理员权限**。
   - 包内已含 Python 服务端、浏览器内核（Chromium）与浏览器扩展，**无需预装 Python / Node / Playwright**。
2. 首次启动会自动初始化数据目录 `%APPDATA%\AksoWorkbench`（若你的电脑上还留着旧的 Mogul 工作台数据库，
   首次会**自动复制接管**，历史会话与账号不丢）。

### 1.2 装载浏览器扩展（一次性，只为「快捷登录」）

「多账号快捷登录」需要把浏览器扩展装进**你自己的 Chrome**。Chrome 在 Windows 上禁止非商店扩展静默安装，
所以这一步必须你亲手点四下（这是平台限制，不是应用偷懒——实证见 [docs/EXTENSION-INSTALL.md](EXTENSION-INSTALL.md)）：

1. 打开**账号中心**（左侧「账号中心」），若顶部出现「未检测到浏览器扩展」提示条 → 点 **一键安装引导**；
   （或：右键托盘图标 → 「安装浏览器扩展…」）
2. 弹窗与浏览器会同时打开：`chrome://extensions` 与扩展所在目录；
3. 在 `chrome://extensions` 右上角打开 **开发者模式**；
4. 点 **加载已解压的扩展程序** → 选中已为你打开的那个目录（**选目录本身，不要进子目录**）。

装好后「在线」徽标与 `Alt+Q` 轮盘即可用；**账号数据由桌面端自动下发，不需要在扩展里另建**。

> 若之后不再看到提示条：那是正常的——连接成功过一次后提示条会**永久静默**
> （以「能否连接成功一次」为判据，之后即使 Chrome 没开也不再打扰你）。

### 1.3 环境要求

| 项 | 要求 |
|---|---|
| 系统 | Windows 10 / 11 x64 |
| 浏览器 | Chrome 或 Edge（要「快捷登录」时才需要） |
| 网络 | 能访问你的 eGMP 平台；自动更新需要能访问 GitHub（受限网络下见 [CONFIG.md](CONFIG.md) §6 的 `proxy.txt`） |

---

## 2. 界面总览

应用有**一个主窗**和**一个轮盘窗**：

- **主窗**：左侧导航（对话 / 设置 / 平台洞察 / 配置工厂 / 账号中心），主区是对话与设置。
- **轮盘窗**：按 `Alt+Q` 呼出（无边框半透明、置顶、**点别处自动关闭**）。
  滚轮或点中心切换盒子；数字键 1–9 直接选人（0 = 第 10 个）；`Esc` 关闭。

| 入口 | 位置 | 用途 |
|---|---|---|
| 账号中心 | 左栏「平台模块」 | 环境 / 账号 / 盒子 / 分配池 / 备份 / 快捷登录 |
| 平台洞察 | 左栏「平台模块」 | 登录验证 / 全量盘点 / 对象理解 / 蜘蛛织网 |
| 配置工厂 | 左栏「平台模块」 | 蓝图校验 / 创建 / AI 生成 |
| 对话 / 设置 | 左栏顶部 | AI 对话、DeepSeek 配置 |
| 托盘菜单 | 任务栏右下角图标右键 | 显示主窗 / 账号轮盘 / 安装扩展 / 检查更新 / 关于 / 退出 |

---

## 3. 账号中心

打开：左栏 → **账号中心**。

### 3.1 站点管理（先加站点）

- 一个**站点 = 一个平台环境**（一个 `base_url`）。
- 粘贴**完整链接**（如 `https://tonbridge-config.aksoegmp.com/admin/...`）会自动清洗为站点，**保留端口**
  （内网非标端口的站点因此也能正常使用）。
- 站点名称可改，**站点地址不可改**（要换地址就新建站点）。
- ⚠️ 删除站点会**级联删除**它下面的全部账号。

### 3.2 添加账号

- 填「站点 + 用户名 + 密码」即可；`盒子` 可留空（= 默认盒子）。
- **页签名（别名）**：可选。填了之后，浏览器里的页签标题与轮盘扇区名都会用它，
  便于在一堆页签里快速定位（如填 `T0901`）。
- **批量添加**：点「批量添加 ▾」，每行一条 `用户名,密码[,盒子]`。
- 密码用 Fernet 加密存本机，**界面永不回显明文**。

### 3.3 账号卡片墙（盒子收纳 + 四态徽标）

- 卡片按**盒子**分组，顶部芯片切换；点芯片右侧可管理盒子（重命名 / 删除 / 禁用 / 新建）。
- **盒子禁用**：禁用后该盒子在**轮盘**中被跳过（账号中心里照常可见可管理）。
- 卡片上的徽标反映**浏览器扩展里的实时状态**（来自扩展上报，几十秒的延迟是正常的）。
- 卡片操作：
  - **快捷登录**：在**你自己的 Chrome** 里以该身份打开并自动登录（见 §4.1）；
  - **打开**：用**应用内置浏览器**打开并免密直达（见 §4.2）；
  - **监听会话**：在托管会话上开始/停止请求录制（见 §8）。
- **批量管理**：开启后可多选 → 移入盒子… / 删除所选。

### 3.4 分配池

- **分配池 = 给平台的「可用账号清单」**：洞察与配置工厂的账号下拉**只列出配置池内的成员**。
- 把常用测试账号加入**配置池**，它们才会出现在洞察/工厂的账号选择里。

### 3.5 备份、导入

| 操作 | 说明 |
|---|---|
| **导出备份** | 导出 JSON，含环境 / 账号（密文口令）/ 盒子配置，**并内嵌解密密钥**。⚠️ 该文件等价于凭据，请妥善保管 |
| **导入备份** | 用文件内密钥解密 → 用本机密钥重新加密 → 落库；按「站点 + 用户名」去重 |
| **env 导入** | 只读扫描原项目（akso-cc / akso-auto）的 `env.json`，把环境与账号导入账号库。**原文件绝不修改** |

### 3.6 页面内轮盘（Ctrl+Shift+Q）

在账号中心页面上按 `Ctrl+Shift+Q`（或点顶部「轮盘 Alt+Q」按钮）会呼出页面内的轮盘浮层——
与桌面 `Alt+Q` 轮盘视觉一致，适合「浏览器里直接打开本页」时使用。

---

## 4. 快捷登录与托管浏览器

这是两条**刻意不同**的路径（设计取舍见 [ADR-0002](adr/0002-browser-allocation-policy.md)）：

| | **快捷登录** | **打开（托管）** |
|---|---|---|
| 用哪个浏览器 | **你自己的 Chrome**（需装扩展） | **应用内置 Chromium** |
| 目的 | 以该身份**真实地干活**（真实 profile / 插件 / 书签） | 可控、可录制、免密、不污染你的浏览器 |
| 自动登录 | 扩展在你的页面里填表 | 内置浏览器里自动填表，成功即**落地免密登录态** |
| 会话保持 | 依赖你的 Chrome 与扩展 | 关闭后仍**免密直达**（下次打开不用再登） |
| 适用于 | 日常点检、演示、需要真实环境的操作 | 需要稳定复现、需要录制、不想动自己浏览器 |

### 4.1 快捷登录（用户 Chrome）

三种触发方式，效果相同：

1. `Alt+Q` 呼出桌面轮盘 → 选账号；
2. 账号中心卡片上的 **快捷登录**；
3. 在浏览器里按扩展快捷键（默认 `Ctrl+Shift+Q`，可在 `chrome://extensions/shortcuts` 改）。

内部流程：桌面写一条指令 → 扩展取到（长轮询，约 20 毫秒）→ 在你的 Chrome 里开页签 →
按账号的隔离规则绑定 → 自动填表登录。

> ⚠️ **Chrome 没运行时扩展不会工作**。桌面在派发前会自动探测并**拉起 Chrome**，
> 所以第一次点会稍慢（冷启动 1~3 秒）。

### 4.2 打开 / 免密直达（内置浏览器）

- 点卡片上的 **打开** → 内置浏览器窗口打开该平台。
  - 首次：自动填表登录 → 成功后**把登录态落盘**；
  - 之后：**免密直达首页**（不用再登录）。
- 会话失效（被平台踢回登录页）时**自动重新登录**（自愈，最多 2 次；再失败会提示检查凭据）。
- **关闭窗口 ≠ 登出**：登录态已保存，下次打开仍免密。
- 要真正登出：用 `POST /api/browser/forget/{账号id}`（或删除
  `%APPDATA%\AksoWorkbench\runtime\browser-states\<账号id>.json`）。

### 4.3 多账号并行（扩展的核心价值）

同一个 Chrome 窗口里可以**同时在线多个账号**，互不串号。这不是靠「多开浏览器」，而是靠**六平面隔离**
逐层切断同源共享资源：

| 平面 | 切断什么 |
|---|---|
| 存储 | `localStorage` 按账号命名空间；`document.cookie` 虚拟化为账号「Cookie 袋」 |
| 鉴权 | 出站 `Authorization: Bearer` 按页签强制改写为该账号的 token |
| Cookie | 出站 `Cookie` 头按账号**回放**（登录时点采集的快照） |
| 缓存 | 同源 GET 追加 `_qlck` 参数，HTTP 缓存按页签硬分区 |
| SW/Cache | 拦截站点 Service Worker 注册；`CacheStorage` 按账号命名空间 |
| IndexedDB | 站点 IndexedDB 按账号前缀化（平台的 `isAdmin` 与菜单树就缓存在共享 IDB 里） |

> 原理与攻防过程见 [docs/EXTENSION-PLANE.md](EXTENSION-PLANE.md)。
> 一句话结论：**官方方案下不同账号不会互相串权**；若你观察到相反现象，请立刻导出诊断（见 §11）。

---

## 5. 平台洞察

打开：左栏 → **平台洞察**。四个命令：

| 命令 | 作用 | 需要填什么 | 产物 |
|---|---|---|---|
| `login` | 只验连通性并预热 token（55 分钟有效） | 账号 | 无 |
| `inventory` | **全量盘点**：发现全部对象 + 深爬字段/布局 | 账号（可选：兜底白名单） | `inventory.json` / `inventory.md` |
| `understand` | **对象三层理解报告**：L1 内容 / L2 关系网 / L3 生命周期流转图 | 账号 + 对象编码（逗号分隔） | 每对象 `*.json` / `*.md` / `*.drawio` |
| `spider` | **五步织网**：跨对象的关系网络（networkx 图分析） | 账号 + 对象编码 | `step1~4` + `spider-network.{drawio,md,json}` |

用法：

1. **账号**下拉选配置池里的账号（见 §3.4）；
2. 选命令；`understand` / `spider` 填对象编码（如 `capa_plan__c,change_control__c`）；
3. 勾选 **understand 启用 LLM 职责摘要**（需已配 DeepSeek Key；不勾选则走确定性标注）；
4. 点 **执行** → 下方实时日志 → 产物区列出文件（`.md`/`.json` 等文本可直接看，其他格式可下载，`.drawio` 用 draw.io 打开）。

**任务历史**在页面底部，可回看每次执行的状态与产物。

> 实现说明：这些命令已**全部用 Python 原生实现**，运行时不调用原项目、不需要 Node。
> 产物写在 `%APPDATA%\AksoWorkbench\runtime\insight\<任务id>\`。

---

## 6. 配置工厂

打开：左栏 → **配置工厂**。它做两件事：**按蓝图创建配置**、**由需求生成蓝图**。

### 6.1 创建（`create`）

1. **① 上传蓝图**：选一个 `blueprint.json` → **上传暂存**（会立即做两层校验并显示问题清单）。
2. **② 执行任务**：选蓝图 + 命令 `create` + **凭证账号**（配置池）；
3. ⚠️ **必须勾选「环境强制确认」**（这是 akso-auto 的原则：由你确认这批写入要落到哪个环境/账号）；
4. 点 **执行** → 日志区实时输出 → 底部任务历史可看状态与**断点**（`checkpoint.json`）。

创建是**幂等**的：重复执行不会重复建对象，已存在的会被跳过。

### 6.2 编排（`orchestrate`）— 用自然语言生成蓝图

1. 命令选 `orchestrate`，在蓝图下拉中选一个**带 `description` 的蓝图**，或用需求描述；
2. 点执行 → 流程：**复杂度评估 → DeepSeek 生成蓝图 → 规范化 + 校验 → 审阅三件套**；
3. 产物：`blueprint.generated.json`、`blueprint.json`、`spec.md`（配置规格）、`checklist.md`（执行清单）；
4. **审阅后**再用 `create` 命令真正执行（生成 ≠ 写入）。

> 需要 DeepSeek API Key；未配置会直接提示。

### 6.3 蓝图的边界

- 上传上限 **10 MB**，必须是 JSON 对象。
- 校验分两层：结构层（schema）+ 规则层（引用完整性、命名规范等），问题分级为 `error` / `warning`。
- 创建涉及对象、字段、选项集、生命周期、工作流、布局、菜单等——各项都按平台真实 API 执行。

---

## 7. 对话与设置

### 7.1 对话

- 主区「对话」：与内置的合规智能体对话（面向药企合规 / 实施问题）。
- 支持流式输出、工具调用（联网读官方页面）、会话历史（左栏「会话记录」）。
- 回答可**点赞/反馈**，用于统计采纳率。

### 7.2 设置

| 项 | 说明 |
|---|---|
| **DeepSeek API Key** | 存本机 SQLite，仅发往 DeepSeek 官方端点。**不填则对话、蓝图生成、洞察 LLM 摘要不可用** |
| **模型选择** | 提供两个选项（经济版 / 旗舰版）。⚠️ 界面**不支持自定义模型名**；若你的账号需要用别的模型名，见 [CONFIG.md](CONFIG.md) §5 |
| **Temperature** | 0（严谨）~ 2（发散），默认 0.7 |

---

## 8. 监听会话（Monitor）

**用途**：搞清楚「在平台上做这个操作，到底发了哪些请求」，为写蓝图/排查提供实证。

1. 在账号中心卡片上点 **开始监听**（会确保该账号的内置浏览器会话在线——可能需要几秒）；
2. 在打开的窗口里**正常操作**平台（建对象、改字段、走审批……）；
3. 点 **停止监听**（或卡片上的同名按钮）→ 产物落
   `%APPDATA%\AksoWorkbench\runtime\monitor\<账号>-<时间戳>\`。

产物内容：请求录制日志（`monitor-log.json`）、断点、降噪统计。录制分三级：**丢弃**（无关流量）、
**精简**（相关但只留摘要）、**完整**（平台业务 API）。

> ⚠️ **危险操作（删除 / 改状态 / 取消 / 停用）会被标记为不可回放**——录制的意义是「看懂」，
> 不是「自动重放」。回放必须逐步人工确认。见 [ADR-0010](adr/0010-monitor-no-dangerous-replay.md)。

---

## 9. 自动更新

- 应用**启动 20 秒后**静默检查一次，之后**每 6 小时**一次；
- 有新版本会**在后台静默下载**（不弹窗、不打断你）；
- **退出应用时自动安装**（不会在你工作途中重启）；
- 想立刻查：托盘右键 → **检查更新…**，或点账号中心右上角的**版本角标**；
- 升级完成后，账号中心会出现「浏览器扩展版本过旧」提示条 → 点 **重新加载引导**，
  在 `chrome://extensions` 里给扩展点一次「重新加载」。

> ⚠️ 升级后若不做「重新加载」，Chrome 里跑的仍是旧版扩展代码——所以这条提示条别忽略。
> 加载/未加载的状态是应用自动比对的（桌面端与扩展同号发布）。

---

## 10. 数据位置与安全须知

### 10.1 你的数据在哪

| 内容 | 位置 |
|---|---|
| 账号 / 环境 / 会话 / 任务记录 | `%APPDATA%\AksoWorkbench\workbench.db`（SQLite 单文件） |
| 洞察与工厂产物 | `%APPDATA%\AksoWorkbench\runtime\insight\<任务id>\`、`.../factory\<蓝图id>\` |
| 托管浏览器的免密登录态 | `%APPDATA%\AksoWorkbench\runtime\browser-states\<账号id>.json` |
| 监听录制产物 | `%APPDATA%\AksoWorkbench\runtime\monitor\...` |
| 浏览器缓存与各账号会话分区 | `%APPDATA%\AksoWorkbench\Partitions\` 等（删掉只会清掉浏览器的登录态，**不会**丢账号） |

**备份建议**：用账号中心的「导出备份」（账号与盒子），产物目录自行复制即可。

### 10.2 安全须知（请知悉）

- **口令加密存本机**（密钥也在本机数据库里），因此**导出备份文件等价于凭据**，别随手发出去。
- 应用的服务只监听**本机回环**（`127.0.0.1`），**没有登录认证**——所以别把端口暴露到局域网，
  也别在多人共用的电脑上长期挂着。
- 浏览器扩展的同步通道同样只在回环上；它的**已知风险窗口**（明文凭据在会话存储里短暂驻留、
  通道无认证）记录在 [ADR-0007](adr/0007-credentials-never-plaintext.md)。
- 真机操作请只在**测试环境**账号上进行。

---

## 11. 常见问题（FAQ）

### 快捷登录点了没反应

1. **Chrome 没开**：桌面会自动拉起，但冷启动要 1~3 秒；再点一次。
2. **扩展没装或没启用**：看账号中心顶部是否有提示条；`chrome://extensions` 里确认扩展存在且为启用态。
3. **扩展是旧版**：出现「浏览器扩展版本过旧」提示条 → 点「重新加载引导」。
4. 仍无反应 → 扩展弹窗右上角 **导出诊断**，把 JSON 发给开发者（里面有每次指令与失败的完整记录）。

### 提示「未检测到浏览器扩展」，但我装了

- 该提示条**只在从未连接成功过**时出现。若它出现，说明扩展尚未成功连上桌面端：
  确认 Chrome 在运行、扩展为启用态，然后重启一次应用（桌面壳会重连）。
- 若你**曾经连上过**却仍看到提示条，说明本地数据库里的「已连接」标记没写进去（DB 权限问题），
  此时按提示重新装载即可，不影响功能。

### 内置浏览器打开后停在「加载中」或一直不登录

- 检查账号密码是否最新（改过平台密码？在账号中心编辑该账号）；
- 该账号权限不足也可能表现成登录后进入受限页；
- 若卡住很久：关闭该会话窗口重开（会自动重登）；仍不行则重启应用
  （可能是浏览器后端卡住，见 [AGENT.md](../AGENT.md) 环境坑）。

### 「打开」能免密，但「快捷登录」仍要我输密码

两条路径是独立的（§4）：**免密登录态只存在于内置浏览器**；你自己的 Chrome 里靠扩展填表，
扩展未装载/未启用时会退化为手动输入。

### 洞察/工厂的账号下拉是空的

账号还没加入**配置池**。到账号中心 → 分配池 → 把账号加入配置池（§3.4）。

### 工厂执行报「需要 UI 明确确认」

勾选「环境强制确认」复选框（§6.1）。这是刻意设计：写配置会落到真实环境。

### 对话报「请先在设置中配置 DeepSeek API Key」

设置页填 Key。蓝图生成（`orchestrate`）与洞察的 LLM 摘要同样需要它。

### 更新一直下载不下来

自动更新要访问 GitHub。先看账号中心版本角标的提示、或托盘「检查更新…」的结论；
若你的网络必须走代理，见 [docs/CONFIG.md](CONFIG.md) §6 的 `proxy.txt`（写一行代理地址即可强制走代理）。

### 应用升级后，功能看起来还是旧的

- 桌面端：完全退出（托盘 → 退出）再启动一次；
- 浏览器扩展：`chrome://extensions` 点一次「重新加载」。

### 想彻底重置

1. 退出应用；
2. 备份/移走 `%APPDATA%\AksoWorkbench`（里面是你的全部数据）；
3. 重新启动应用（会新建空数据库）。注意：**账号口令一旦丢失无法恢复**，请先导出备份。
