# 迁移台账

> 每个转化文件的状态：**封装中 / 已重写 / TS 版退役**。重写一个模块 = 对应 TS 能力标记维护模式。
> 四原项目冻结不动；阶段 1/2 只封装与迁移既有知识，重写只在阶段 3 按台账触发。

## fork 基线

- 基线仓库：`D:\ai_assistant\mogul_simulator`（mogul/ 包，Python FastAPI 版，`__version__=0.4.0`，「检索收敛」后的稳定节点）。
- fork 动作：整包复制 → `workbench/`，改包名/端口/数据目录，DB 兼容接管 mogul.db。
- 四原项目冻结锚点：以当前工作区内容为基线（原仓库未打 git tag 的以目录快照为准，**不回写原仓库**）。

## 阶段 1A：fork mogul

| 源（mogul_simulator/mogul/） | 目标（workbench/） | 状态 |
|---|---|---|
| main.py config.py db.py __init__.py | 同名 | 已重写（包名/环境变量/迁移 7-8） |
| api/routes_{chat,conversations,knowledge,settings,sync}.py | api/ 同名 | 已 fork |
| harness/ 7 文件 | harness/ | 已 fork |
| services/ 8 文件 | services/ | 已 fork |
| static/ index.html + css 6 + js 6 + fonts | static/ | 已 fork + 模块导航 |
| 迁移 1-6（内嵌 db.py MIGRATIONS） | db.py | 已 fork |

## 阶段 1B：聚合层（封装）

| # | 文件 | 封装对象 | 状态 |
|---|---|---|---|
| 1.1 | services/proc.py | ——（新建） | 已重写 |
| 1.2 | services/modules.py + adapters/*.json | ——（新建） | 已重写 |
| 1.3 | api/routes_modules.py | ——（新建） | 已重写 |
| 1.4 | api/routes_insight.py | akso-cc login/inventory/understand/spider | 已重写（封装） |
| 1.5 | api/routes_factory.py | akso-auto create/orchestrate/monitor | 已重写（封装） |
| 1.6 | static/pages/{factory,insight}.html + js + css | ——（新建） | 已重写 |
| 1.7 | shell/shell.py | ——（新建） | 已重写 |
| 1.8 | tests/test_proc.py 等 | ——（新建） | 已重写 |

## 阶段 2A：统一账号库

| # | 文件 | 知识来源 | 状态 |
|---|---|---|---|
| 2.1 | db.py 迁移 7（platform_env/account） | quick-login storage/db.ts 模型 + 两项目 env 字段 | 已重写 |
| 2.2 | services/accounts.py | quick-login background/core/credentials.ts 加密思路 → Fernet | 已重写 |
| 2.3 | api/routes_accounts.py | —— | 已重写 |
| 2.4 | static/pages/accounts.html + js/css | UI 范式学习 AdsPower 卡片墙 | 已重写 |
| 2.5 | tools/import_accounts.py | 三原项目 env 文件读取（原文件不动） | 已重写 |

## 阶段 2B：托管浏览器 + 自动登录引擎

| # | 文件 | 从 quick-login 迁移的知识 | 状态 |
|---|---|---|---|
| 2.6 | services/browser_pool.py | parallel-session.ts「账号↔页签」→「账号↔context」 | 已重写 |
| 2.7 | services/autologin.py | auto-login.ts 节奏门控全量 + fillPasswordInIframes | 已重写 |
| 2.8 | services/autologin.py（token 捕获） | site-auth.ts / parallel-session.ts captureToken | 已重写 |
| 2.9 | api/routes_browser.py | navigation.ts + tab-title.ts | 已重写 |
| 2.10 | static/pages/browser.html + js/css | —— | 已重写 |
| 2.11 | tests/test_autologin.py | tmp/verify-fillrhythm.mjs 9 项断言等价迁移 | 已重写 |
| 2.12 | 不迁清单 | shield-main/shield-bridge/tab-rules/parallel-session(七平面)/wheel-*/popup-* | 不迁（扩展专属） |

## 阶段 3：深度 Python 化（egmp/）—— 已全量完成（2026-09-07）

> 状态变更：**routes_insight / routes_factory 已切原生调用**，运行时不再依赖 node 与两个原仓库；
> adapters/*.json 降级为只读参考存档。真机验收：login / understand / spider 均在真实平台跑通（egmp-native）。

| 源 | 目标 | 状态 |
|---|---|---|
| akso-cc queries.ts + akso-auto openapi-queries.js | egmp/queries.py | **已重写**（TS 版退役） |
| akso-cc crawler/{fields,fragments,layouts,object-discovery}.ts | egmp/insight/crawler.py | **已重写**（对象发现链实证口径保留） |
| akso-cc assemble.ts + report.ts | egmp/insight/{assemble,report}.py | **已重写** |
| akso-cc understand/{index,annotate,relations,lifecycle,flowgraph,drawio,render}.ts | egmp/insight/{understand,annotate,relations,lifecycle,flowgraph,drawio,render}.py | **已重写**（LLM 职责摘要经回调委托 mogul DeepSeek 客户端） |
| akso-cc spider/{index,step2,step3,step4,step5}.ts | egmp/insight/spider.py | **已重写**（step5 图分析用 networkx——9/4 调研收益点兑现） |
| akso-auto validate-blueprint.js + blueprint-schema.json(ajv) | egmp/writers/blueprint.py | **已重写**（pydantic 两层校验替代 ajv） |
| akso-auto spec-generator.js | egmp/writers/blueprint.py（generate_review_spec/checklist） | **已重写** |
| akso-auto api/idempotency.js | egmp/writers/idempotency.py | **已重写** |
| akso-auto create-object.js | egmp/writers/objects.py | **已重写** |
| akso-auto create-field.js + field-router.js | egmp/writers/fields.py | **已重写**（并发 5 批次口径保留） |
| akso-auto create-picklist.js | egmp/writers/picklists.py | **已重写**（创建后回查 ID 口径保留） |
| akso-auto create-lifecycle-status.js + lifecycle-builder.js 等 | egmp/writers/lifecycle.py | **已重写**（固定 workflowCancelStatus UUID 等实证值保留） |
| akso-auto create-workflow.js + create-workflow-step.js + workflow-builder.js + save-workflow-start-step.js + save-lifecycle-user-action.js | egmp/writers/workflows.py | **已重写**（a-i 管道/两次提交连线口径保留） |
| akso-auto save-form-layout.js + form-layout-builder.js + save-list-layout.js + list-layout-builder.js | egmp/writers/layouts.py | **已重写**（全量替换须先读合并的实证语义保留） |
| akso-auto create-menu.js | egmp/writers/menus.py | **已重写** |
| akso-auto orchestrate.js | egmp/orchestrate.py | **已重写**（8 步对象管道/拓扑排序/checkpoint 落盘） |
| akso-auto gmp/complexity.js | egmp/complexity.py | **已重写**（规则忠实移植） |
| akso-auto gmp/blueprint-generator.js（36.8KB 关键词库） | egmp/generate.py | **融合改写**（见下注） |
| akso-auto monitor/monitor.js + query-layer.js + param-* | egmp/monitor/{monitor,query_layer}.py | **已重写**（载体换 playwright-python） |
| akso-auto monitor/{interpreter,reproduce,feedback,iterate}.js | egmp/monitor/interpreter.py | **已重写**（API_MAP 对位 Python writers；复现逐步授权/DANGEROUS 不回放纪律继承） |
| akso-auto monitor/{self-recovery,exporter,cleaner}.js | 未迁（骨架声明） | 维护模式：按需（token 续期由 egmp client 缓存机制部分承担） |

**融合改写注**（generate.py）：蓝图 JSON 生成不再移植 akso-auto 的确定性关键词库，
改由 mogul DeepSeek 客户端按结构化提示词生成 → normalize_blueprint 规范化（补
__c/snake_case/系统字段）→ validate_blueprint 两层校验（失败带 issues 回炉）。
理由：全家桶灵魂 = mogul harness 即 LLM 底座；确定性关键词库为上游冻结资产。
复杂度评估仍为确定性移植。

## 不迁清单（永远或长期）

| 文件 | 理由 |
|---|---|
| quick-login 全部 ui/、manifest、service-worker.ts、shield-*、tab-rules.ts、parallel-session.ts（七平面）、wheel-*、popup-* | 扩展形态专属，在原仓库长期维护 |
| akso-auto common/browser-manager.js、api/env-manager.js | 被 browser_pool / 统一账号库替代 |
| akso-cc main.ts、akso-auto main.js（入口壳） | 被 routes_insight / routes_factory 替代 |
| mogul_simulator src-tauri/、dist/、node_modules/ | Tauri 壳退役，Python 版为唯一形态 |
